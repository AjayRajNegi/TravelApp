import {View, Text} from 'react-native';
import React from 'react';

const GameScreen = () => {
  return (
    <View style={{flex: 1, alignItems: 'center', justifyContent: 'center'}}>
      <Text style={{color: 'black'}}>GameScreen</Text>
    </View>
  );
};

export default GameScreen;
